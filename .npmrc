# The build toolchain (wrangler, the OpenNext adapter, typescript) lives in
# devDependencies. A build image that sets NODE_ENV=production would make npm
# skip exactly those and fail the build with a missing binary, so pin the
# behaviour here rather than depending on the image's environment.
include=dev
